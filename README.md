### dev
Create a new project:
```
npm i -D typescript vite @types/react
npm i tailwindcss @tailwindcss/vite @stomp/stompjs
```

Install dependencies (`node_modules`) for existing project:
```
npm i
```

Run the project:
```
npm run start
```
